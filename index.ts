import * as _ from 'lodash';

console.log('hello world')

type Style = 'bold' | 'italic';

let font: Style;

font = 'bold'

interface Person{
    first: string;
    last: string;
}

const person: Person ={
    first: 'Eric',
    last:'Walter'
}